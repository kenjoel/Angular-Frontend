import { VoteCountDirective } from './vote-count.directive';

describe('VoteCountDirective', () => {
  it('should create an instance', () => {
    const directive = new VoteCountDirective();
    expect(directive).toBeTruthy();
  });
});
